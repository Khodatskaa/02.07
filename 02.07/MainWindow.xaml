<Window x:Class="_02._07.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_02._07"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListBox x:Name="lstBooks" HorizontalAlignment="Left" Height="500" VerticalAlignment="Top" Width="300" Margin="10,10,0,0" SelectionChanged="lstBooks_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding CoverUrl}" Width="50" Height="75" Margin="5"/>
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox x:Name="txtBookContent" HorizontalAlignment="Left" Height="500" VerticalAlignment="Top" Width="460" Margin="320,10,10,10" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" IsReadOnly="True"/>
        <Button x:Name="btnLoadBooks" Content="Load Popular Books" HorizontalAlignment="Left" VerticalAlignment="Top" Width="300" Margin="10,520,0,0" Click="btnLoadBooks_Click"/>
    </Grid>
</Window>
